/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { X_MIX_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_ROOT_DISCRIMINATOR = new Uint8Array([
  58, 195, 57, 246, 116, 198, 170, 138,
]);

export function getUpdateRootDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_ROOT_DISCRIMINATOR);
}

export type UpdateRootInstruction<
  TProgram extends string = typeof X_MIX_PROGRAM_ADDRESS,
  TAccountRelayer extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRelayer extends string
        ? WritableSignerAccount<TAccountRelayer> &
            AccountSignerMeta<TAccountRelayer>
        : TAccountRelayer,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateRootInstructionData = {
  discriminator: ReadonlyUint8Array;
  root: ReadonlyUint8Array;
};

export type UpdateRootInstructionDataArgs = { root: ReadonlyUint8Array };

export function getUpdateRootInstructionDataEncoder(): FixedSizeEncoder<UpdateRootInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['root', fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_ROOT_DISCRIMINATOR })
  );
}

export function getUpdateRootInstructionDataDecoder(): FixedSizeDecoder<UpdateRootInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['root', fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getUpdateRootInstructionDataCodec(): FixedSizeCodec<
  UpdateRootInstructionDataArgs,
  UpdateRootInstructionData
> {
  return combineCodec(
    getUpdateRootInstructionDataEncoder(),
    getUpdateRootInstructionDataDecoder()
  );
}

export type UpdateRootInput<
  TAccountRelayer extends string = string,
  TAccountPool extends string = string,
> = {
  relayer: TransactionSigner<TAccountRelayer>;
  pool: Address<TAccountPool>;
  root: UpdateRootInstructionDataArgs['root'];
};

export function getUpdateRootInstruction<
  TAccountRelayer extends string,
  TAccountPool extends string,
  TProgramAddress extends Address = typeof X_MIX_PROGRAM_ADDRESS,
>(
  input: UpdateRootInput<TAccountRelayer, TAccountPool>,
  config?: { programAddress?: TProgramAddress }
): UpdateRootInstruction<TProgramAddress, TAccountRelayer, TAccountPool> {
  // Program address.
  const programAddress = config?.programAddress ?? X_MIX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    relayer: { value: input.relayer ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.relayer), getAccountMeta(accounts.pool)],
    data: getUpdateRootInstructionDataEncoder().encode(
      args as UpdateRootInstructionDataArgs
    ),
    programAddress,
  } as UpdateRootInstruction<TProgramAddress, TAccountRelayer, TAccountPool>);
}

export type ParsedUpdateRootInstruction<
  TProgram extends string = typeof X_MIX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    relayer: TAccountMetas[0];
    pool: TAccountMetas[1];
  };
  data: UpdateRootInstructionData;
};

export function parseUpdateRootInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateRootInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { relayer: getNextAccount(), pool: getNextAccount() },
    data: getUpdateRootInstructionDataDecoder().decode(instruction.data),
  };
}
